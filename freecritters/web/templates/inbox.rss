<?xml version="1.0"?>
<rss version="2.0">
    <channel>
        <title>{{ fc.site_name|e }} mail</title>
        <link>{{ fc.url('mail.inbox')|e }}</link>
        <description>{{ fc.username|e }}'s mail on {{ fc.site_name|e }}.</description>
        <pubDate>{{ last_change|rsstimestamp|e }}</pubDate>
        <lastBuildDate>{{ last_change|rsstimestamp|e }}</lastBuildDate>
        <docs>http://blogs.law.harvard.edu/tech/rss</docs>
        <generator>freeCritters</generator>
        {%- for message in messages %}
        {%- set message_url = '%s%s%s'|format(fc.url('mail.conversation', None, True, conversation_id=message.conversation_id), '#mailmessage', message.message_id) %}
        <item>
            <title>
                {{- message.subject|e -}}
                {% if message.user != none %} from {{ message.user.username|e }}{% endif -%}
            </title>
            <description>{{ message.message|e }}</description>
            <link>{{ message_url|e }}</link>
            <pubDate>{{ message.sent|rsstimestamp|e }}</pubDate>
            <guid>{{ message_url|e }}</guid>
        </item>
        {%- endfor %}
    </channel>
</rss>